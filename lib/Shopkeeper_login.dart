import 'package:flutter/material.dart';

class ShopkeeperLogin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Shopkeeper")),
      body: const Center(child: Text("Shopkeeper Page")),
    );
  }
}
